import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>Lilie's Landscaping</h1>
      {/* Add navigation links if needed */}
    </header>
  );
};

export default Header;
